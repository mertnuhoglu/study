; --------------------------------------------------------------------------------
; eval (current-form): (lvar "foo")
foo_5708
; --------------------------------------------------------------------------------
; eval (current-form): (walk {} (lvar "s"))
s_5711
; --------------------------------------------------------------------------------
; eval (current-form): (unify {} (lvar "s") 42)
{s_5714 42}
; --------------------------------------------------------------------------------
; eval (current-form): (let [s (lvar "s")] (walk {s 42} s))
42
; --------------------------------------------------------------------------------
; eval (current-form): (let [s (lvar "s") v (lvar "v")] (walk {s v v 42} s))
42
; --------------------------------------------------------------------------------
; eval (current-form): (unify {} (lvar "s") (lvar "v"))
{s_5724 v_5725}
; --------------------------------------------------------------------------------
; eval (current-form): (unify {} 1 2)
false
; --------------------------------------------------------------------------------
; eval (current-form): (unify {} 1 1)
{}
; --------------------------------------------------------------------------------
; eval (current-form): (== 1 2)
#function[logic-tutorials.episode1/==/fn--5697]
; --------------------------------------------------------------------------------
; eval (current-form): (== 1 2)
#function[logic-tutorials.episode1/==/fn--5697]
; --------------------------------------------------------------------------------
; eval (current-form): (== (lvar "s") (lvar "v") )
#function[logic-tutorials.episode1/==/fn--5697]
; --------------------------------------------------------------------------------
; eval (root-form): (comment (lvar "foo") ;; => foo_14417 (walk {} (lvar "s")) ;; => s_14420 (unify {...
nil
; --------------------------------------------------------------------------------
; eval (current-form): ((== (lvar "s") (lvar "v") ) {})
[{s_5740 v_5741}]
; --------------------------------------------------------------------------------
; eval (current-form): (walk {} (lvar "s"))
s_5744
; --------------------------------------------------------------------------------
; eval (current-form): (let [state (unify {} (lvar "s") 42)] (walk state (lvar "s")))
s_5748
; --------------------------------------------------------------------------------
; eval (current-form): (unify {} (lvar "s") 42)
{s_5751 42}
; --------------------------------------------------------------------------------
; eval (current-form): (let [s (lvar "s")] (walk s {s 42}))
{s_5754 42}
; --------------------------------------------------------------------------------
; eval (current-form): (let [s (lvar "s")] (walk s {s 42}))
{s_5757 42}
; --------------------------------------------------------------------------------
; eval (current-form): (let [state (unify {} (lvar "s") 42)] (walk state (lvar "s")))
s_5761
