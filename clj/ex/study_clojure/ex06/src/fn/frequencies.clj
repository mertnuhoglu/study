(ns fn.frequencies)

; rfr: video/20230222-mert-clj-egzersiz-48.mp4

; (frequencies coll)    #nclk/orta-seviye
; Returns a map from distinct items in coll to the number of times
; they appear.
;

(frequencies ['a 'b 'a 'a])
;{a 3, b 1}
; a sembolünden 3 tane varmış
; b sembolündense 1 tane varmış

